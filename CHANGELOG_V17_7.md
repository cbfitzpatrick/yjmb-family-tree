# v17.7

- Restyled the tree-page bug-report trigger to exactly match the circular Info/Reload controls: same size/alignment, no shadow, neutral icon color, and matching hover tint.
- Kept the bug trigger SVG-only so there is no duplicate emoji/icon.
- Anchored browser and Admin card-name blocks to the true horizontal center; reducing usable width for icon clearance now shrinks text without shifting it to either side.
- Expanded Admin Mode → Feature Preview with a standalone 150×80 sample-card generator. Admins can type arbitrary displayed first/last names and instrument/section text and preview the resulting section color immediately without locating a real person in the tree.
- Added independent toggles for every prepared card icon on the standalone sample card, with Section Leader enabled by default as an immediate example.
- Retained the existing gallery plus the ability to preview icons/placement guides on real Admin tree cards.

No workbook schema, Worker endpoint, encrypted tree payload format, or public icon-enable flag changed in v17.7.
